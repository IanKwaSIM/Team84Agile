<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Weekly Challenges | Fitness App</title>
    <link rel="stylesheet" href="/styles.css" />
    <script defer src="/general.js"></script>
    <script defer src="/challenges.js"></script>
    <script defer src="/auth.js"></script>
  </head>
  <body>
    <!-- Persistent Top Bar -->
    <nav class="navbar">
      <div class="nav-left">
        <span class="menu-icon">☰</span>
        <h1 class="logo">ELEVATE</h1>
      </div>
      <!-- Login / Logout button on nav bar -->
      <div class="nav-right">
        <% if (user) { %>
          <button id="logout-btn" class="login-btn">Logout</button>
        <% } else { %>
          <button id="login-btn" class="login-btn">Login</button>
        <% } %>
      </div>
    </nav>

    <!-- Sidebar Menu -->
    <div class="sidebar">
      <button class="close-btn">✖</button>
      <ul class="sidebar-menu">
        <% if (user) { %>
        <li><a href="/">Home</a></li>
        <li><a href="/account">My Fitness</a></li>
        <li><a href="/track-workout">Track Workout</a></li>
        <li><a href="#">Social</a></li>
        <li><a href="/challenges">Weekly Challenge</a></li>
        <li><a href="/gymlocator">Gym Locator</a></li>
        <li><a href="/logout">Logout</a></li>
        <% } %>
      </ul>
      <div class="settings-icon">⚙️</div>
    </div>

    <!-- views/challenges.ejs -->
    <div class="challenge-container">
      <!-- Main Challenge Section (Centered) -->
      <main class="challenge-section">
          <h1>Current Weekly Challenge</h1>
          <p class="challenge-text">Loading challenge...</p>
      </main>

      <!-- Leaderboard Section (Right Side) -->
      <aside class="leaderboard">
          <h2>Leaderboard 🏆</h2>
          <ul id="leaderboard-list">
              <li>Loading...</li>
          </ul>
      </aside>
  </div>
  </body>
</html>
